# Service level objectives

Generated by `scripts/slo_report.py` from `src/hugin/slo.py` on 2026-08-13. Do not
edit this file; edit the objectives and regenerate. The objectives are the ones
Airflow enforces - `hugin_daily.slo_check` calls the same `evaluate()` this
page renders - so the document cannot describe a policy the pipeline does not
have.

## What an objective is here

Three things, all required: a **number** the table must satisfy, the **SQL**
that measures it, and a sentence saying **what breaks** when it does not. A
threshold with no measurement is a wish. A measurement with no stated
consequence is a dashboard.

Three dimensions, failing differently:

* **Freshness** - how far the table's newest business date lags the replay
  clock (BR-01), in *replay* days. A table current as of replay day 400 is
  fresh even though the date on it is 2009. A negative value means the table is
  ahead of the clock, which a completed backfill makes normal.
* **Completeness** - row floors and non-null rates. A table that lost 90% of
  its rows still answers every query and still passes every `not_null` test on
  the rows that remain. dbt asks whether each row is legal; this asks whether
  enough of them are here.
* **Coverage** - BR-12 identity resolution, from `mart_identity_coverage`.
  The one metric that may legitimately fall when a new delivery arrives with
  names nothing recognises, which is why it is an objective and not a test.

## Where the thresholds come from

Floors sit **below** the measured value with headroom, so an objective does not
fail on legitimate variation. Every `Basis` column below names the measurement
it came from, so the next person to tighten one can see what they are
tightening against.

Three objectives are **deliberately not met** and are marked *known*. They
encode what a table is supposed to contain rather than what it currently does.
They are reported on every run and do not fail the DAG - an objective written
down as unmet is not an incident, and treating it as one teaches everyone to
ignore a red DAG, which is how the real breach gets missed.

## Enforcement

`hugin_daily` runs `slo_check` after `dbt_test`:

    ingest_bronze -> dbt_run_silver_gold -> dbt_test -> slo_check -> soda_scan

A blocking breach raises, the task turns red, and the run fails. Outside the
DAG:

    python -m hugin.slo                          # exit 1 on a blocking breach
    python -m hugin.slo --include-known-breaches # exit 1 on any breach
    python -m hugin.slo --json docs/slo-check.json

## Last measurement

Taken 2026-08-13, at replay date **2009-06-14**: **13 met, 3 known
breaches, 0 blocking.** Raw numbers in `docs/slo-check.json`.

## Freshness

| Objective | Table | Target | Last reading | Consequence of breach | Basis |
|---|---|---|---|---|---|
| `freshness` | `gold.fct_production_daily` | <= 2 replay days | -2,665 (ok) | Every production figure downstream - mart_well_performance, the allocation reconciliation, the Metabase dashboard - is answering about a day that has already passed, and no query will say so. | Two replay days of slack: one for the run itself, one for a retry. A negative value means the table is ahead of the clock, which a backfill makes normal and is not a breach. |
| `freshness` | `gold.fct_production_monthly` | <= 62 replay days | -2,636 (ok) | BR-02's reconciliation compares daily against monthly. A stale monthly side makes every variance look like a data problem in the daily side. | Two months of slack, because the reported monthly figure for a month only exists after that month has closed. A tighter threshold would fail every month-end by construction. |

## Completeness

| Objective | Table | Target | Last reading | Consequence of breach | Basis |
|---|---|---|---|---|---|
| `row_floor` | `gold.fct_production_daily` | >= 1.4e+04 rows | 1.486e+04 (ok) | Silently missing production days. Every aggregate stays valid-looking and every total is low. | Measured 14,859 on 2026-08-13; floor set ~6% below, with headroom for a partial replay. |
| `wellbore_key_resolved` | `gold.fct_production_daily` | >= 100 % of rows | 100 (ok) | A production row with no wellbore key joins to nothing and disappears from every per-well figure while still counting in the total. BR-12 routes unresolved identities to a surrogate key rather than to NULL precisely so this stays at 100. | Measured 100% on 2026-08-13. This one is exact on purpose: the model guarantees it. |
| `row_floor` | `gold.fct_production_monthly` | >= 480 rows | 497 (ok) | BR-02 loses the side it reconciles against. | Measured 497 on 2026-08-13. |
| `wellbore_floor` | `gold.dim_wellbore` | >= 7 wellbores | 7 (ok) | A missing wellbore takes its whole production history out of every per-well report, and the field total still looks plausible. | Measured 7 distinct wellbore_uid across 18 SCD2 versions on 2026-08-13. |
| `exactly_one_current_version` | `gold.dim_wellbore` | <= 0 wellbores in breach | 0 (ok) | Two current versions double-counts every fact joined to the dimension; zero current versions drops them. Both look like a production change rather than a dimension bug. | Structural. assert_gold_scd2_has_exactly_one_current_version.sql tests the same rule per build; this watches it between builds. |
| `covers_field_life` | `gold.dim_date` | >= 3,044 days | 3,044 (ok) | dim_date is deliberately built over field life rather than over loaded data, so a join against it never drops a fact for a day nobody has ingested yet. A short spine silently reintroduces that. | 2008-06-01 to 2016-09-30 inclusive is 3,044 days. Exact, not a floor: the spine is generated rather than loaded. |
| `row_floor` | `gold.fct_log_sample` | >= 3e+04 rows | 3.042e+04 (ok) | Log curves vanish from the depth-range queries without any error. | Measured 30,421 on 2026-08-13 under the bounded LAS load (--max-las-files 8). The floor tracks the bounded load, not the full one. |
| `curve_key_resolved` | `gold.fct_log_sample` | >= 99 % of rows | 0 (known) | Without a curve key, a sample cannot be joined to what the curve is - its unit, its description, whether the mnemonic means different things in different files. The readings are there and unusable. | What the model is for. See known_breach. |
| `row_floor` | `gold.dim_curve` | >= 1 rows | 0 (known) | An empty curve dimension is what makes the curve_key breach above possible. It is listed separately because the cause is here and the symptom is there. | Any non-empty dimension. fct_log_sample holds 196 distinct mnemonics, so the true figure should be 196. |
| `row_floor` | `gold.fct_trajectory` | >= 450 stations | 475 (ok) | BR-09's minimum-curvature path is computed per station. Missing stations shorten the wellbore without making the geometry look wrong. | Measured 475 stations across 2 trajectories on 2026-08-13. |
| `crs_declared` | `gold.fct_trajectory` | >= 100 % of rows | 0 (known) | BR-10 and the rule in CLAUDE.md: never assume a CRS. A station with no declared CRS is a coordinate whose datum is a guess, and in the North Sea the ED50/WGS84 guess is wrong by hundreds of metres. | What BR-10 requires. See known_breach for why it is not met, and why that is not a bug. |
| `row_floor` | `gold.fct_simulation` | >= 180 rows | 183 (ok) | mart_sim_vs_actual loses the simulated side of its comparison. | Measured 183 on 2026-08-13, from a single Eclipse report date. |

## Coverage - BR-12

| Objective | Table | Target | Last reading | Consequence of breach | Basis |
|---|---|---|---|---|---|
| `total` | `mart.mart_identity_coverage` | >= 95 % resolved | 100 (ok) | Below this, enough wellbore identities are unresolved that per-well figures are missing production that the field total still contains. The gap is the kind that reads as a well being shut in. | Measured 100% across 44 identities on 2026-08-13. The objective is set at 95 rather than 100 deliberately: BR-12 is allowed to fail to resolve a name, and recording that is the whole design. What is not allowed is failing quietly, or at scale. |
| `no_system_below_half` | `mart.mart_identity_coverage` | <= 0 source systems in breach | 0 (ok) | The total can stay healthy while one source system resolves almost nothing - WITSML contributes 2 identities and PROD contributes 14, so a total-only objective would not notice WITSML failing completely. | Measured: every source system at 100% on 2026-08-13. |

## Known breaches, with diagnosis

Each of these is an objective the pipeline does not meet today. They are listed here rather than lowered to fit, because an objective adjusted to match a broken number stops being an objective.


### `gold.fct_log_sample.curve_key_resolved`

**Target**: >= 99 % of rows. **Consequence if it stays this way**: Without a curve key, a sample cannot be joined to what the curve is - its unit, its description, whether the mnemonic means different things in different files. The readings are there and unusable.

0% as of 2026-08-13. bronze.las_curve_header is empty, so silver_log_curve and dim_curve are empty, so the join in fct_log_sample yields NULL for every row. The reader exists and is registered (LasCurveHeaderReader in hugin.ingestion.las, wired in load_job.all_readers) and emits only on the static load date; the static load that populated las_sample did not populate it. Diagnosis is in docs/slo.md; the fix is a reload, not a code change.


### `gold.dim_curve.row_floor`

**Target**: >= 1 rows. **Consequence if it stays this way**: An empty curve dimension is what makes the curve_key breach above possible. It is listed separately because the cause is here and the symptom is there.

0 rows as of 2026-08-13. Same root cause as gold.fct_log_sample.curve_key_resolved: bronze.las_curve_header is empty.


### `gold.fct_trajectory.crs_declared`

**Target**: >= 100 % of rows. **Consequence if it stays this way**: BR-10 and the rule in CLAUDE.md: never assume a CRS. A station with no declared CRS is a coordinate whose datum is a guess, and in the North Sea the ED50/WGS84 guess is wrong by hundreds of metres.

0% as of 2026-08-13, and the cause is in the source rather than in the code. The directional survey files declare no CRS, and fct_trajectory says so in its header: northing_offset_m and easting_offset_m are offsets from the well reference point, not projected coordinates, which is why they are named _offset_. There is nothing to declare a CRS about until a surface location with a datum is read from another source. The objective is kept at 100% and left breached rather than lowered, because lowering it would encode the absence as acceptable, and the day a projected coordinate does arrive it must carry its datum.

## What has no SLO, and why

- **`gold.fct_drilling_state`.** Built from `silver.drilling_telemetry`, which
  Spark owns and which holds calibrated fixture data rather than Volve
  telemetry. A freshness objective over fixture timestamps would measure the
  fixture generator. It gets one on the day real WITSML telemetry streams
  through, and not before.
- **`mart_*` row counts.** They are derived from the gold tables that do have
  objectives. A mart floor would fail second and add nothing to the diagnosis.
- **Query latency.** That is `docs/performance.md` and SPEC.md section 13.
  Mixing a latency target into a data-quality objective makes both harder to
  read and makes it unclear which one paged you.
